# فهم وتشغيل المشروع

## التشغيل

1. افتح مجلد المشروع، ونفّذ `npm ci` مرة بعد تنزيل هذه النسخة، ثم شغّل `Start-Local.cmd` أو اكتب `npm start` في الطرفية داخله.
2. افتح http://localhost:3000 في المتصفح.
3. أنشئ حسابًا تجريبيًا بكلمة مرور من 12 حرفًا على الأقل.
4. أضف منتجًا وسجّل طلبًا، ثم افتح My orders.
5. أعد تشغيل السيرفر: الحساب والطلب يظلان محفوظين في قاعدة البيانات.

لا توجد عملية دفع حقيقية. تسجيل الطلب هنا يحفظه بحالة انتظار الدفع فقط.

## ما الذي جعله Full Stack؟

- الواجهة: صفحات HTML وتنسيق CSS وتفاعل JavaScript.
- السيرفر: Node.js يستقبل الطلبات ويتحقق منها ويحدد المستخدم المسموح له بالوصول.
- قاعدة البيانات: SQLite تحفظ البيانات محليًا، وPostgreSQL على Neon يحفظ بيانات النسخة المنشورة عند إضافة DATABASE_URL.

خطوات النشر المجاني موضحة في [DEPLOY-AR.md](DEPLOY-AR.md). حسابات جهازك لا تنتقل تلقائيًا إلى النسخة المنشورة.

عند التسجيل، الواجهة ترسل البيانات إلى `/api/register`. السيرفر يتحقق منها، ويخزن تجزئة كلمة المرور مع salt عشوائي، ثم ينشئ جلسة. المتصفح يستقبل Cookie لا تستطيع JavaScript قراءتها.

عند تسجيل طلب، المتصفح يرسل معرف المنتج والكمية فقط. السيرفر يقرأ الأسعار من قاعدة البيانات ويحسب الإجمالي، ثم يحفظ الطلب وتفاصيله داخل معاملة واحدة. المستخدم الآخر لا يستطيع قراءة هذا الطلب من صفحة طلباته.

## ترتيب القراءة

1. اقرأ دالة `api` في `public/app.js` لفهم انتقال البيانات.
2. تابع `/api/register` و`/api/login` في `server.js` لفهم الحسابات.
3. اقرأ `currentUser` و`requireUser` لفهم الجلسات والصلاحيات.
4. تابع `/api/orders` لفهم حساب السعر والمعاملة ومنع التكرار.
5. شغّل `npm test` واقرأ `test/api.test.js`؛ اختبرنا الرفض أيضًا، وليس النجاح فقط.

## وصف دقيق لمعرض الأعمال

تطوير مشروع ويب Full Stack باستخدام HTML وCSS وJavaScript وNode.js وSQLite، يتضمن إنشاء الحسابات وجلسات الدخول، وعرض المنتجات من قاعدة البيانات، وحفظ الطلبات وعرض سجل خاص بكل مستخدم، وحفظ رسائل التواصل. أُضيفت اختبارات للتحقق من الحسابات والصلاحيات وصحة حساب الأسعار واستمرار البيانات بعد إعادة التشغيل. المشروع نسخة تطبيقية دون بوابة دفع أو توصيل منتجات.

هذه نسخة مطوّرة بمساعدة Codex. لفهمها وتقديم خدمة مشابهة للعميل، جرّب تغيير منتج وإضافة حقل صحيح واختبار الوصول بحسابين واشرح رحلة الطلب من الواجهة إلى قاعدة البيانات.
