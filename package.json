{
  "name": "future-tech-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Future Tech portfolio with Node.js, local SQLite and hosted PostgreSQL",
  "engines": {
    "node": ">=22.14.0 <23"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "build": "node --check server.js && node --check database.js && node --check netlify-adapter.js"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
