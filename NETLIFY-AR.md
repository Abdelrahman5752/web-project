# نشر Future Tech على Netlify وNeon

خطة Netlify Free لا تتطلب بطاقة بحسب إعلان المنصة، ولها حد استخدام شهري. راجع الخطة وقت التسجيل: https://www.netlify.com/pricing/

1. افتح https://app.netlify.com/signup وسجّل باستخدام GitHub واختر Free.
2. اختر إضافة مشروع ثم Import an existing project، واختر GitHub ومستودع `Abdelrahman5752/web-project` وفرع `main`.
3. الإعدادات موجودة في `netlify.toml`: أمر البناء `npm run build`، ومجلد النشر `public`، ومجلد الدوال `netlify/functions`. اترك Base directory فارغًا.
4. أضف متغير البيئة `DATABASE_URL`، وقيمته رابط الاتصال الجديد من Neon. استخدم الرابط الكامل الذي يبدأ بـ `postgresql://`، دون أمر `psql` أو علامات اقتباس محيطة أو شرطات هروب أضافها تنسيق النص. اترك معاملات SSL كما هي. لا تضع الرابط في GitHub أو الصور أو المحادثة.
5. اجعل المتغير متاحًا للدوال (Functions)، أو لجميع النطاقات إذا كان هذا هو الاختيار المتاح. ثم انشر المشروع. إذا أضفت المتغير بعد النشر فأعد النشر لتطبيقه.
6. افتح رابط الموقع الأساسي الذي توفره Netlify. المتغير `URL` توفره المنصة تلقائيًا؛ لا تحتاج إلى إضافة PORT أو HOST أو APP_ORIGIN.
7. افتح `/api/health` ثم جرّب حسابًا تجريبيًا، وأضف منتجًا وطلبًا وراجع My orders. سجّل الخروج والدخول للتأكد من بقاء الطلب.

استخدم عنوان الموقع الأساسي أثناء الاختبار. روابط معاينة الفروع قد ترفض طلبات الكتابة لأن التحقق من مصدر الطلب يستخدم العنوان الأساسي. عند استعمال نطاق مخصص يمكن ضبط `APP_ORIGIN` على عنوان HTTPS الدقيق ثم إعادة النشر.

الصور والصفحات تُخدم مباشرة، والبيانات تُحفظ في Neon. لا توجد مدفوعات حقيقية. إذا ظهرت رسالة تعذر الخدمة، تحقق من DATABASE_URL وسجلات الدالة دون مشاركة أسرار الاتصال.

تم اختبار مسارات الدالة والجلسات والتسجيل والطلبات محليًا، مع اختبارات PostgreSQL محلية؛ نجاح النشر والاتصال الفعلي بـNeon يحتاج إلى التحقق بعد النشر. حدود الطلبات في الذاكرة تخص نسخة الدالة الواحدة، وليست حدًا موزعًا بين جميع النسخ.
